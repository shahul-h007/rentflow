import { NextResponse } from "next/server";
import { ZodError } from "zod";
export function apiError(error:unknown){if(error instanceof ZodError)return NextResponse.json({error:"Invalid request",issues:error.flatten()},{status:400});const message=error instanceof Error?error.message:"Unexpected server error";const status=message==="Unauthorized"?401:message.includes("access")||message.includes("permission")?403:message.includes("configured")?503:message.includes("not found")||message.includes("must")||message.includes("exceeds")||message.includes("already")?400:500;return NextResponse.json({error:message},{status})}
